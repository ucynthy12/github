import { GitUserDirective } from './git-user.directive';

describe('GitUserDirective', () => {
  it('should create an instance', () => {
    const directive = new GitUserDirective();
    expect(directive).toBeTruthy();
  });
});
